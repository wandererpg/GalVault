---
title: GalVault 知识库介绍
summary: GalVault 是一个用于检索、筛选和核对 Galgame 与视觉小说资料的 Obsidian 知识库。（每半年进行一次更新）
updated: 2026-08-23
---

## 概览

GalVault 用于整理 Galgame、视觉小说及其衍生，重点支持以下工作：

- 根据年份、篇幅、评分、标签、主角身份和作品属性筛选作品；
- 核对作品的发行版本、成人向/全年龄版本、官方语言和动画改编情况；
- 查询会社、创作者、角色和声优之间的关联；
- 在中文名、日文名、英文名或 VNDB 标识之间交叉定位作品；
- 为逐步追加线索的“猜作品”任务提供可追溯的候选和排除依据。

当前知识库快照约包含：

| 类型 | 数量（约） |
| --- | ---: |
| 作品 | 2,390 |
| 发行版 | 11,913 |
| 会社 | 1,293 |
| 创作者 | 11,999 |
| 角色 | 15,507 |
| 标签 | 2,551 |

数据以本地文件为主，来源标识主要保留 VNDB 信息。评分、排名、发行状态等可能随外部站点更新而变化。

## 目录结构

| 目录 | 内容 | 典型用途 |
| --- | --- | --- |
| `作品` | 每部视觉小说的主条目 | 查作品年份、标签、评分、角色、声优和改编信息 |
| `发行版` | 具体 PC、主机、下载版、续作或语言版本 | 核对发行日期、平台、年龄分级和语言 |
| `会社` | 制作公司、发行商和相关组织 | 查询公司作品、开发/发行关系 |
| `人物` | 编剧、原画、音乐、脚本等人员 | 查询人员参与过的作品 |
| `角色` | 角色及其声优、作品关联 | 核对主角、女主角、配角和配音信息 |
| `标签` | 标签定义和标签关联 | 解释“傲娇”“教师主角”“分支剧情”等筛选条件 |
| `系统` | 验证报告和系统性文件 | 查看同步或检查结果 |
| `assets/covers` | 经 WebP 压缩的作品封面 | 在作品页面中显示封面 |
| `assets/characters` | 经 WebP 压缩的角色画像 | 在角色页面中显示人物图 |

根目录中的 `.base` 文件是 Obsidian Bases 视图，用来展示常用的筛选表格，例如作品总览、角色与声优、系列与版本等。

## 作品条目的主要字段

作品页面通常包含 YAML 元数据和若干自动生成区块。常见字段如下：

| 字段 | 含义 |
| --- | --- |
| `entity_id` | 本地实体标识，通常包含 VNDB work ID |
| `display_title` | 当前页面使用的显示标题 |
| `title_zh` / `title_ja` | 中文名和日文名（若已收录） |
| `first_release_date` | 作品首次发行日期，不一定等于某个移植版日期 |
| `release_count` | 已关联的发行版本数量 |
| `has_all_ages_release` | 是否存在全年龄版本 |
| `has_adult_release` | 是否存在成人向版本 |
| `ratings` | 外部站点评分，例如 VNDB 评分 |
| `ranks` | 外部站点排名 |
| `source_ids` / `source_urls` | 来源站点的 ID 和链接 |
| `AUTO:BEGIN tag` 区块 | 自动同步的作品标签 |
| `AUTO:BEGIN character` 区块 | 自动同步的角色关联 |
| `AUTO:BEGIN voice` 区块 | 自动同步的声优关联 |
| `AUTO:BEGIN adaptation` 区块 | 动画等改编关联 |
| `AUTO:BEGIN relation` 区块 | 前作、续作、FD 等作品关系 |

页面中的 `[[双括号链接]]` 是 Obsidian 内部链接。点击标签、角色、声优或公司名称，可以继续查看相关实体。

## 基本用法

### 1. 直接搜索作品

在 Obsidian 全局搜索中，可以使用目录和关键词缩小范围：

```text
path:"10-作品" "Teacher Protagonist"
path:"10-作品" "Branching Plot" "Tsundere Heroine"
path:"10-作品" "first_release_date: \"2017"
```

如果中文名没有结果，可以改用日文名、英文名、罗马字名或页面中的 `vndb` 标识搜索。

### 2. 使用 Bases 视图

从根目录打开对应的 `.base` 文件，可以按字段排序、筛选和浏览。常用视图包括：

- `作品总览.base`：按年份、评分、类型和标签查看作品；
- `系列与版本.base`：查看正篇、续作、FD 和发行版关系；
- `角色与声优.base`：查询角色和配音演员；
- `会社与创作者.base`：查询制作公司与创作者；
- `三站榜单.base`：对照不同站点的评分或排名；
- `审核中心.base`：集中查看需要复核的条目。

### 3. 进行作品筛选

建议先把用户线索分成“硬条件”和“软条件”：

1. 硬条件：年份、正篇/续作、成人向、主角身份、是否奇幻、是否有动画改编等；
2. 标签条件：家庭、日常、都市、校园、分支剧情、傲娇、恋爱等；
3. 软条件：评分高低、篇幅感受、知名度、翻译情况等；
4. 对候选条目逐项记录“命中”“冲突”“未确认”，不要把缺失数据直接当成否定证据。

推荐的筛选顺序是：

```text
年份 → 正篇/系列关系 → 主角身份 → 成人/全年龄 → 篇幅 → 标签 → 评分 → 动画与语言核对
```

## 推荐的记录格式

在回答“根据线索猜作品”时，可以使用下面的结构，方便复核：

```markdown
## 当前候选

### 作品名
- 命中：2017 年、教师男主、校园、恋爱、分支剧情
- 冲突：评分偏低；官方中文状态待确认
- 未确认：篇幅、动画改编
- 证据：作品页面、发行版页面、角色/声优页面

### 排除项
- 作品 A：年份不符
- 作品 B：主角是高中生，不是教师
```

## 数据核对原则

### “有全年龄版本”不等于“不是成人向”

不少作品同时拥有成人版和全年龄移植版。判断“非全年龄”时，应确认用户指的是目标发行版，还是要求作品完全不存在全年龄版本。

### “没有动画记录”不等于绝对没有动画化

如果作品的动画改编区块为空，只能说明当前知识库没有记录。遇到关键候选时，应再检查官方页面、制作公司公告或可靠数据库。

### “没有官方中文”要和民间汉化区分

发行版中出现中文补丁、民间汉化或转载页面，不代表作品有官方中文。应优先查看发行商、平台商店和官方版本的语言列表。

### 评分和排名是动态信息

评分会随投票数和站点算法变化。知识库中的评分适合做相对筛选，不应当视为永久固定值。

### 标签可能存在翻译差异

同一概念可能对应多个标签，例如“都市”可能表现为 `Modern Day`、`Modern Day Earth` 或 `Fictional Modern Day Japanese Town`；“日常”也可能与 `Slice of Life Drama` 或 `Slice of Life Comedy` 区分。筛选时应结合标签原文和剧情简介判断。

## 维护规则

- 不要删除或改写 `<!-- AUTO:BEGIN ... -->` 与 `<!-- AUTO:END ... -->` 之间的自动区块；
- 需要补充人工说明时，写在自动区块之外，并注明来源和核对日期；
- 修改作品标题或别名时，尽量保留 `source_ids` 和 `source_urls`；
- 修订角色中文名时，在 `系统/角色中文化/中文名修订.csv` 中记录，避免直接修改后被下次同步覆盖；
- 新增条目后，检查页面内部链接是否能跳转到对应的公司、人物、角色和标签页面；
- 同步或批量更新后，查看 `99-系统/同步报告/validation.md`；
- 对“待审核”“需要复核”状态的页面，优先核对标题、日期、语言、动画化和系列关系。

### 更新封面和角色画像

新增或同步作品、角色页面后，运行：

```powershell
python 系统/工具/import_vndb_images.py
```

脚本根据页面中的 VNDB ID 查询图片，转换为 WebP，并增加 Obsidian 图片嵌入。已有图片会被复用，索引、清单、错误记录和导入报告保存在 `系统/图片导入`。更多参数见 `系统/图片导入说明.md`。

### 更新角色中文标题

角色页面使用中文 `display_title` 和 H1 作为显示标题，同时把罗马音、日文原名及 VNDB 别名保存在 `aliases`。文件名仍为原罗马音，以保证现有 Wikilink 稳定。

首次运行先安装依赖：

```powershell
python -m pip install -r 系统/工具/character-name-requirements.txt
python 系统/工具/localize_character_names.py
```

中文标题包含日文汉字转简体、词典翻译和机器音译，均不代表官方中文译名。需要纠正时，在 `系统/角色中文化/中文名修订.csv` 填写 VNDB 角色 ID 与中文名，再重新运行脚本。分析、规则和结果报告保存在 `系统/角色中文化`。

## 知识库的适用范围

本库适合做作品检索、标签交叉筛选、系列关系整理和资料核对。它不是完整的剧情攻略、评测数据库或下载工具；资源获取、版权状态和外部网站可用性需要另行确认。

当本地资料无法确认英文名与中文名的对应关系、官方中文状态或动画化信息时，应使用可靠的外部来源进行补充核查，并在结论中明确区分“已确认”和“推测”。
